import React from 'react';

class SubmitButton extends React.Component {

    render() {
        return (<button type="button" class="btn btn-info">Add</button>);
    }

}

const styles = {
    submitButton : {
        
    }
};

export default SubmitButton;